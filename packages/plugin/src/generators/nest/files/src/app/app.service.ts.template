import { Injectable } from '@nestjs/common';
import { ExampleAnswerDto } from './example.dto';

@Injectable()
export class AppService {
  getData(name: string): ExampleAnswerDto {
    return { message: `Hello, ${name}!` };
  }
}
